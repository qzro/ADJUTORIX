{
  "check": {
    "commands": ["npm test"],
    "allowWrite": false
  },
  "fix": {
    "commands": ["npm run lint -- --fix"],
    "allowWrite": true
  },
  "verify": {
    "commands": ["npm test"],
    "allowWrite": false
  },
  "deploy": {
    "commands": ["echo \"deploy not configured\""],
    "allowWrite": false,
    "requireConfirm": true
  }
}
