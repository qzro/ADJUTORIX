name: ADJUTORIX v0.9.0 Operator Unified Control Spine Finality

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  finality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Assert ADJUTORIX v0.9.0 operator unified control spine finality
        run: node scripts/release/assert-adjutorix-v090-operator-unified-control-spine-finality.mjs
